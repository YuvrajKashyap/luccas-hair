# Roadmap

## Shipped

- Product strategy, sitemap, user flows, content requirements, and design direction
- Next.js App Router application with responsive production UI
- Verified Square service, price, duration, hours, and direct booking handoff
- Honest coming-soon product and policy states
- Metadata, local-business schema, sitemap, robots, analytics, and performance instrumentation
- Optional Supabase auth/admin/event architecture with fail-closed behavior
- Regression tests, CI verification, dependency hardening, screenshots, and Vercel deployment

## Next, when evidence exists

1. Replace atmospheric concept visuals with Tony-approved photography.
2. Add services, policies, suite/postal details, or a custom domain only after direct confirmation.
3. Activate and verify the private Supabase workflow if Tony needs analytics access.
4. Define products, inventory, payments, fulfillment, and returns before implementing commerce.
5. Add Google Business and review surfaces only when their verified URLs and real reviews are available.

## Release rule

Every future release must preserve the Square booking path, keep unconfirmed facts out of public copy, and pass `npm run verify`.
